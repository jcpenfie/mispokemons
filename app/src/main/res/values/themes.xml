<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MisPokemons" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorSecondary">@color/yellow</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="BottomNavStyle">
        <item name="android:background">@color/red</item> <!-- Fondo del BottomNavigationView -->
        <item name="itemIconTint">@color/black</item> <!-- Color de los iconos -->
        <item name="itemTextColor">@color/white</item> <!-- Color del texto -->
    </style>

    <style name="Theme.MisPokemons" parent="Base.Theme.MisPokemons" />
</resources>